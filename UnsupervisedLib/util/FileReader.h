#ifndef vector
#include <vector>
#endif


class freader {
public:
	template <typename T>
	const std::vector<std::vector<T>> read();

	/*
	read from txt files. 
	read from csv files.
	parsing char is comma for default but user can change it
	if he wants so.
	*/
};